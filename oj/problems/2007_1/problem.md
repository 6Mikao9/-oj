# 2007_1: 统计空格回车TAB

【问题描述】
从输入的字符串中，统计空格、回车、TAB出现的次数。

【输入格式】
输入一行字符串（可能包含空格、制表符），以回车结束。
注意：需要统计的是整个输入中的字符数量。

【输出格式】
输出三个整数，用逗号分隔：
第一个：空格(' ')的数量
第二个：回车换行('
')的数量
第三个：制表符('	')的数量

【样例输入】
Hello World	Test

【样例输出】
1,1,1

【提示】
- 使用getchar()逐个读取字符直到EOF
- 空格是' '，回车是'\n'，TAB是'\t'
